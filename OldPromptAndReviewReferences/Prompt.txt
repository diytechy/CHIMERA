Prompt:

I want to create a table that maps variables between yml files that configure world generation for the Terra plugin for minecraft.

I would like to generate this using a powershell script, but python can also be considered.

Input:

A folder path to a Terra project.

Output:

A csv file with the following information:

BiomeID, Extends, Color, the climate it's assigned to.

Each row would correspond to a biome file (stored in the folder "biomes").

A valid biome file can be verified by seeing the "type: BIOME" key-value pair.

BiomeID would be found from the ID key in the biome file.

Extends would be found from the "extends" key in the biome file.

Color would be found from the "color" key in the biome file.

The climate would be 

1. Add any ocean biomes (set in oceans.yml) that are not currently set in set_biomes_in_climates.yml to set_biomes_in_climates.yml. Attempt to add them to the group they would be associated with based on the name and climate semantics.  Ex: ARCTIC_ARCHES likely should be placed where the ocean is shallow and cold (polar-shallow-ocean), ICEBERG_OCEAN should likely be set where the ocean is deep and cold (polar-deep-ocean and boreal-ocean), STONEGATE_SEAS should be placed where the ocean is shallow, regardless of temperature.

2. Add any biomes from fill_temperature_zones.yml that are not currently set in set_biomes_in_climates.yml using similar methods.  Ex: FROSTY_FINGERS would likely be placed somewhere like polar-coast-flat, polar-coast, polar-vast-forest-coast, perhaps boreal-vast-forest-coast.

Weights should also be drafted according to the likelihood those biomes would occur with respect to other biomes.  Remember, biomes can be set in more than one group.

What does the "abstract: true" flag do in biome files, for example, from "abstract\environment\land\dry\environment_land_dry_desert.yml".  Note the Terra project that reads this configuration is located in "C:\Projects\Terra" and may have context for what this flag is used for.

Thank you for explaining. It looks like the biome table (BiomeTable.csv) now only includes biomes that flow back to one of the presets. Please update the respective table generation scripts to ensure the table also includes any valid biomes (a valid biome can be defined in any yml file in the biome folder that has a biome type without an abstract flag) that are not in any of the presets and that they are listed at 0%. Additionally, I see a list of known_intermediate_names, but this is not necessary, as the stages are processed; each designated biome label should eventually result in a final valid biome designation. Thus, the intermediate biomes can be derived as a part of the script, and should not be explicit.

pillowplains is an intermediate biome that's created in "fill_temperature_zones.yml" that's further propagated into PILLOW_PLAINS_INNER and others using "fill_temperature_zones.yml".  , along with some other biomes.  I would like 

In the rearth preset, there are some special biomes that are distributed as a part of spread_temperature_zones, and further into fill_patchwork, and finally through the border_biomes.yml  (Ex: PILLOW_PLAINS).  I would like to pull those biomes into origen2 preset.  Can you make updates to the origen2 preset in order to:

1. Add any biomes and intermittent biomes from spread_temperature_zones into appropriate biome breakdowns in precipitation.yml and or elevation.yml.

2. Add the following files from the rearth preset to the origen2 preset to breakdown the biomes from spread_temperature_zones that are intermediates:

        - << biome-distribution/stages/fill_patchwork.yml:stages

        - << biome-distribution/stages/special/border_biomes.yml:stages

		

3. Verify all the biomes that occur in rearth also occur in origen2

Make a script (preferably powershell) can compare two folders (Folder A (Source) and Folder B (Destination)).  For each common folder between those folders, compare all content, including files in subfolders, and determine the following:

 1. The file name that is being checked

 2. The fRootA complete folder path to Folder A

 3. The relative path from root of Folder A to the file in question.  Ex: If the file is "Folder B\Houses\Birds\FileK.abc", the relative path would be "Houses\Birds"

 4. The state of the file when compared to the destination:

  - MULTIPLE: The file exists more than once in Folder A or more than once in Folder B, so a comparison cannot be performed reliably.

  - EXACT MATCH: Both path and file match in Folder B

  - FILE MOVED: Content matches, but the subfolder location in Folder B is different.

  - FILE DIFFERENT: File name exists in Folder B, but the content does not match

  - FILE MISSING: The file name does not exist in Folder B.

 5. The RootB complete folder path to Folder B if the file in question did exist in the Folder B subfolder (EXACT MATCH / FILE MOVED / FILE DIFFERENT)

 6. The relative path from Folder B to the file in question if it did exist (EXACT MATCH / FILE MOVED / FILE DIFFERENT)

If the Folder A path is not provided, assume it is "C:\Projects\Hydraxia2", if the folder B path is not provided, assume it is "C:\Projects\ORIGEN2"

Make a script (Preferably powershell) that reads in a CSV (formatted from the previous step) and copies and modifies the listed files if the conditions are right.

Inputs:

1. Path to CSV (default to .\hydraxia_vs_origen2.csv)

2. String array listing the priority folder labels to go through first.  (default to ["structures","features","biomes","biome-distribution"])

3. A string replace Table with "ReplaceFrom" and "RepalceTo" fields. (default with the following form:

ReplaceFrom, RepalceTo
"features/ores/distribution.yml", "features/geological/deposits/distribution_hydraxia.yml"
"technical_crap/", ""
"BASE", "BASE_HYDRAXIA"
)

Read the CSV and if applicable, sort RelativePathA first so that higher priority folders (the folder names of relative folders that should be checked first), are checked first, followed alphabetically.

For each file in the ordered list:

If the Status column says "FILE MISSING", copy the file from its full path (RootA+RelativePathA+FileName) to the equivalent path in folder B (RootB + RelativePathA + FileName).  Replacement must be case sensitive.

After copying, modify the content of the file so that any content of the file that matches any entry in the "ReplaceFrom" entry of the string replace table is replaced with the corresponding "ReplaceTo" field.

Exit the script early if 10 files are copied.

Create a new script or add functionality to "calculate_biome_percentages.py" which combines all the samplers that are used each of the files into a single large "samplers" structure along with their variables resolved so that no files are being referenced.

The top level expression will be a simple expression which I can further modify to evaluate the resolved samplers.  This is what I would expect the top of the output to be:
"
type: EXPRESSION
expression: y
samplers:
"
Followed by all the samplers and their resolved constants / external samplers / ect so that the file contains all the expressions without any references to another file, an example of one of the samplers would be:
"

  continents: &continents
    dimensions: 2
    type: EXPRESSION
    expression: |
      max(
        max(
          -sampler(x / scale / globalScale, z / scale / globalScale) * spread + offset,
          spawnIsland(x, z)
        ),
        // Set continentalness to spotContinentalFactor in center of spots, 0 on spot edges,
        // and decrease quadratically moving further outwards
        spotContinentalFactor * (1-(spotDistance(x,z)/spotRadius(x,z))^2))
    variables:
      globalScale: 1
      scale: 2
      offset: 0.1
      spread: 1
      spotContinentalFactor: 0.3
    samplers:
      spawnIsland:
        dimensions: 2
        type: DOMAIN_WARP
        amplitude: 150
        warp:
          type: FBM
          octaves: 2
          sampler:
            type: TRANSLATE
            x: 5000
            z: -5000
            sampler:
              type: OPEN_SIMPLEX_2
              frequency: 0.004
        sampler:
"


New error on output:

Configuration: Noise Config
	Message: Data provided is not a double. Data is type: String
	Path: samplers.continents...samplers.spotDistance...sampler.samplers.noise...frequency
	Full Path: 
		From configuration "Noise Config"
		In entry "samplers"
		In entry "continents"
		In entry "."
		With type "EXPRESSION"
		In entry "samplers"
		In entry "spotDistance"
		In entry "."
		With type "DOMAIN_WARP"
		In entry "sampler"
		With type "EXPRESSION"
		In entry "samplers"
		In entry "noise"
		In entry "."
		With type "CELLULAR"
		In entry "frequency"

Update this script to evaluate noise configuration constants as well.  For example, frequency should not be shown as "1 / 1000", but should be evaluated to "0.001" before the output is saved.

Configuration: Noise Config
	Message: Data provided is not a long. Data is type: String
	Path: samplers.rawElevation...samplers.noise...sampler.samplers.mountainMask...sampler.salt
	Full Path: 
		From configuration "Noise Config"
		In entry "samplers"
		In entry "rawElevation"
		In entry "."
		With type "EXPRESSION"
		In entry "samplers"
		In entry "noise"
		In entry "."
		With type "TRANSLATE"
		In entry "sampler"
		With type "EXPRESSION"
		In entry "samplers"
		In entry "mountainMask"
		In entry "."
		With type "PROBABILITY"
		In entry "sampler"
		With type "OPEN_SIMPLEX_2"
		In entry "salt"

                Configuration: Noise Config
	Message: Data provided is not a boolean. Data is type: Double
	Path: samplers.spotBaseElevation...salt-lookup
	Full Path: 
		From configuration "Noise Config"
		In entry "samplers"
		In entry "spotBaseElevation"
		In entry "."
		With type "CELLULAR"
		In entry "salt-lookup"

The "salt-lookup" entry should still be boolean (true / false), and not converted to floats.

Global view is typically -5000 to 5000 blocks, but noise tool only allows about 0 - 400

Add a global render scaler near the top of the python tool for configurability with a default value of 20 that takes all frequency values and multiplies them by this render scaler, and takes all amplitudes and divides it by this render scaler.

In the documentation (https://terra.polydev.org/config/documentation/objects/NoiseSampler.html#cellular), Terra indicates "Terra will automatically determine the thresholds and segment the noise behind the scenes, making weighted lists easy to configure."

Can you show in the code how the distributor determines how to space out noise filter feedback?  The terra source-code which you have worked on previously is at "C:\Projects\Terra"
For example, from "C:\Projects\ORIGEN2\biome-distribution\stages\climate\temperature.yml"
  - type: REPLACE_LIST
    sampler:
      type: EXPRESSION
      expression: temperature(x, z)
    default-from: land
    default-to:
      - ice-cap: &iceCap         1
      - tundra: &tundra         1
      - boreal-snowy: &borealSnowy    1
      - boreal-cold: &borealCold     1
      - boreal-warm: &borealWarm     1
      - boreal-hot: &borealHot      1
      - temperate-cold: &temperateCold  1
      - temperate-warm: &temperateWarm  3
      - temperate-hot: &temperateHot   2
      - tropical-savanna-wet: &tropicalCold   1
      - tropical-monsoon: &tropicalWarm   1
      - tropical-rainforest: &tropicalHot    4

How does Terra compute the corresponding temperature thresholds to distribute the list to?  Is it just -1 to 1?  Or is there some other computation being performed?

Document your findings to detail how the distributor works, and how it changes when the normalize flag is set.

#########################################################

Note how the climate is calculated using the stages of temperature, elevation, and precipitation (Multiple presets use the cascading stage set:

- << biome-distribution/stages/climate/temperature.yml:stages
- << biome-distribution/stages/climate/precipitation.yml:stages
- << biome-distribution/stages/climate/elevation.yml:stages
)

While this does eventually result in the high level biome selection to set in climate (like "polar-crater-lake" and "boreal-cold-dry"), the cascading calculations at each stage take quite a long time to complete, and each stage is recalculating a relatively complex set of calculations (that originate from all of them recalculating "continents").This is all used to create a map topology with a somewhat realistic climate related to emulated precipitation, temperature, and elevation, and further set appropriate biomes in that climate.

I would like to explore 2 alternatives, although there may be better alternatives to what I propose.

Note: The elevation sampler is required no matter what as it is used inside other functions.

1. Create a new stage that combines all 3 climate factors into a single stage, and performs the biome distribution in one pass.  This would require creating a new sampler that outputs a combined climate value based on the 3 factors, and then distributing biomes based on that combined value.  This would likely be the fastest method, but would require significant changes to the biome distribution system.  A separate elevation function would still be required for other systems, but a single climate function could build that elevation computation in along with the others to simplify the expression and prevent chaining the same calculation over and over.

2. Design a new map topology equation, especially for elevation, which could perform more quickly.  Potentially modify the temperature / precipitation expressions to also operate more quickly, or see if there is a way to modify the distribution itself to reduce or eliminate the need for temperature and precipitation to be dependent on elevation.  This elevation could be used separately to calculate the continental boundaries as well.  The elevation topology does not need to be very detailed as that is handled in further stages down the pipeline, but it could drop below "sea-level" (0) within a continent to create larger bodies of water inside the main continent, as well as creating islands in larger bodies of water.  The noise functions available in Terra (and subsequently Seismic / Paralithic) would be necessary to generate this topology.

############################################################3

I will first evaluate in the NoiseTool, but to do this I need some fixes in
  .scripts\resolve_samplers.py.  Update .scripts\resolve_samplers.py for the following:
  
  1. Retain the first few lines of the file (.artifacts\resolved_samplers.yml) with the expressions / commented expressions so they can be used easily for evaluation.  Only replace the section "samplers" and below with the resolved sampler list.
  2. Make sure to retain aliases / references to samplers.  You removed this before when resolving constants and rules for some of the datatype, but this was unnecessary and to evaluate the cache type I'm quite sure it will need to reference a single existing cache type sampler for each dependent sampler to rely on.

  ######################################################################

  The cached version took significantly longer to render, I assume there is some       
  limitation in the implementation that is slowing it down.  Please proceed to phase   
  3, ideally as a script that could be run which would take in the continent,spawn     
  island,  temperature, precipitation, and elevation samplers, ect  and create
  combined samplers for use in respective locations, like "spawn-island-continent" ,   
  "spawn-island-continent-elevation", "spawn-island-continent-elevation-temperature", and "combined climate", and shuffle the "set biomes in climate" yml files to use the respective distribution from the combined climate sampler, as that will now be overloaded with multiple dimensions in a single parameter.

  ######################################################

  In the function "generate_combined_sampler_yml" within
  "generate_combined_climate.py" it appears to just write out a previously computed    
  combined sampler for climate.  Ideally this function would actually read the core    
  climate sampler files from math\samplers (spawnIsland / spots / continents / elevation / temperature / precipitation) and iteratively reduce / resolve the functions (while  retaining constants) until the final expressions remain to populate combined_climate.yml.

  ############################################################33

 Using SamplerResolver is not appropriate in this case for two reasons:

 1. It does not maintain variable links with their source files.  Ex: These variables were resolved but for the combined_climate.yml this is not necessary and prevents easy iteration in the future.  These lines should not have been removed along with many others:
 
      globalScale: $customization.yml:global-scale
      scale: $customization.yml:continental-scale
      offset: $customization.yml:continental-offset
      spread: $customization.yml:continental-spread

 2. It does not actually mathematically simplify the expression.  For example, "combined_climate.yml" now has 1000 extra lines (6x bigger) and has expressions evaluated multiple times for each sampler.  The sampler "combinedContinent" performs "parabolicMap(in, middleRadius/outerRadius, 0, 1,                       -1)" multiple times.  This can be mathematically simplified to prevent duplicate computations.

 generate_combined_climate should be updated or a new file function should be used due to the special needs it has which differentiate it from SamplerResolver.

 ###############################################################

Closer, but some generated content is still coming from hard-coded sources.  Ex:

generate_combined_climate.py Line 159: "continentNoise = -continentSampler(x / contScale / globalScale, z / contScale / globalScale)"

This line should have been automatically constructed by looking at the continent.yml sampler, seeing it's interdependencies with the other core samplers (spawnIsland / spots / continents / elevation / temperature / precipitation) deriving common / reused expressions, and deriving the corresponding expressions to finalize in 
combined_climate.yml.

The main thing to note is if any of the core climate files is changed (spawnIsland / spots / continents / elevation / temperature / precipitation), rerunning generate_combined_climate.py should recheck for interdependent / repetitive calculations, reform expressions, and produce final mathematically simplified expressions.