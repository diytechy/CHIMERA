id: EQ_SINKHOLE_OUTER
type: BIOME
abstract: true

vars: &variables
  base: ${meta.yml:ocean-level} + 5

terrain:
  sampler:
    dimensions: 2
    type: EXPRESSION
    variables: *variables
    expression: '-y + base

      '
