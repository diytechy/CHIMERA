id: EQ_FLAT
type: BIOME
abstract: true


# Perfectly flat terrain.

vars: &variables
  base: $customization.yml:terrain-base-y-level

terrain:
  sampler:
    type: EXPRESSION
    dimensions: 3
    variables: *variables
    expression: -y + base + elevationDetailedBlockHeight(x,z) + (simplex(x, z)+1)/6 * biomeInfluence(x,z)

  sampler-2d:
    dimensions: 2
    type: CONSTANT
    value: 0
